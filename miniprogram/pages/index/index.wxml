<view class="header">
  <view class="search-container">
    <view>深圳市</view>
    <view class="input-box">
      <icon type="search" size="14" />
      <input type="text" bindconfirm="inputSearch" value="{{queryFilde.keyword}}" maxlength="10" placeholder="请输入关键字" confirm-type="search" />
      <view class="input-clear">+</view>
    </view>
  </view>
  <view class="filter-box">
    <view class="filter-item" catchtap="regionPulldown">
      <view>{{region}}</view>
      <view class="pulldown-icon">∟</view>
      <view class="{{isPulldown[0] ? '.region-container' : '.pulldown-hidden'}}">
        <view class="left-box">
          <view class="left-item">{{filterItem[0][0]}}</view>
        </view>
        <view class="right-box">
          <view class="right-item" bindtap="districtClick" data-index="{{index}}" wx:for="{{filterItem[0][1]}}" wx:key="index">{{item}}</view>
        </view>
      </view>
    </view>
    <view class="filter-item" catchtap="rentPulldown">
      <view>租金</view>
      <view class="pulldown-icon">∟</view>
      <view class="{{isPulldown[1] ? '.rent-container' : '.pulldown-hidden'}}">
        <view class="rent-box">
          <text class="section__title">租金：</text>
          <slider bindchange="sliderEvent" min="200" max="1500" backgroundColor="#3f989d" show-value></slider>  
          <button class="confirm-btn radio-btn" catchtap="rentConfirm">确定</button>
        </view>
      </view>
    </view>
    <view class="filter-item" catchtap="selectPulldown">
      <view>筛选</view>
      <view class="pulldown-icon">∟</view>
      <view class="{{isPulldown[2] ? '.select-container' : '.pulldown-hidden'}}">
        <view class="select-box">
          <form class="select-radio" bindsubmit="selectSubmit">
            <view class="radio-item">
              <view class="select-lable">性别：</view>          
              <radio-group bindchange="sexSelect">
                <label wx:for="{{limitSexList}}" wx:key="index" for="{{index}}" class="{{limitSex[index] ? '.limit-sex-active' : ''}}" catchtap="radioClick" data-index="{{index}}"><radio value="{{item}}" name="sex"/>{{item}}</label>
              </radio-group>
            </view>
            <view class="radio-item">
              <view class="select-lable">类型：</view>
              <checkbox-group bindchange="checkboxSelected">
                <label catchtap="checkboxClick" data-index="{{index}}" wx:for="{{checkboxList}}" wx:key="index" class="{{item[0] ? '.limit-sex-active' : ''}}">
                    <checkbox value="{{item[1]}}" checked="{{item[0]}}" name="{{item[1]}}"/>{{item[1]}}
                </label>            
              </checkbox-group>
            </view>
            <button class="confirm-btn select-btn"  form-type="submit">确定</button>
          </form>
        </view>
      </view>
    </view>
    <view class="filter-item" catchtap="sortPulldown">
      <view>排序</view>
      <view class="pulldown-icon">∟</view>
      <view class="{{isPulldown[3] ? '.sort-container' : '.pulldown-hidden'}}">
        <view wx:for="{{sortList}}" wx:key="index" catchtap="sortEvent" data-index="{{index}}">{{item}}</view>
      </view>
    </view>
  </view>

</view>


<!-- 辅助元素，用于点击取消下拉菜单的显示 -->
<view class="{{isPulldown[0] || isPulldown[1] || isPulldown[2] || isPulldown[3] ? '.mask': '.mask-hidden'}}" catchtap="pulldownHidden"></view>
<scroll-view
  class="list-view"
  scroll-y="{{true}}"
  bindscrolltolower="reachBottomLoad"
  scroll-top="{{scrollTop}}"
  enable-back-to-top="{{true}}"
  enhanced
  show-scrollbar="{{false}}"
>
  <InfoCard list="{{list}}"/>
</scroll-view>

<button catchtap="tempBtn" class="test-btn">回到顶部</button>

<!-- <view catchtap="getValue" style="background-color: aqua; z-index: 10;">hello</view> -->







